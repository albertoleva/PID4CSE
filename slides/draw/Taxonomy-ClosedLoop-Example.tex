\tikzstyle{block} = [draw, rectangle, minimum height=2.5em, minimum width=2.8em]
\tikzstyle{sum} = [draw, circle, node distance=0]
\tikzstyle{input} = [coordinate]
\tikzstyle{output} = [coordinate]
\tikzstyle{junction} =[circle,inner sep=1pt,minimum size=0.1mm,fill=black,draw=black]

\begin{tikzpicture} [auto, node distance=2cm]

\def\colC{green!40!black};
\def\fillC{green!20!white};

\def\colS{magenta!80!black};
\def\fillS{magenta!10!white};

\def\colE{blue!60!black};
\def\fillE{cyan!20!white};

\node [block,thick,
       draw=\colC,fill=\fillC,
       minimum width=1.4cm,minimum height=1cm]
       (C) {\textcolor{\colC}{$A=K(R-O)$}};

\node[left of=C,\colE,
      node distance=1.8cm,anchor=east]
      (SP) {$R=1$};

\draw[->,thick,\colE]
      (SP.east) -- (C.west);

\node [block,thick,right of=C,
       node distance=3.4cm,
       draw=\colS,fill=\fillS,
       minimum width=1.4cm,minimum height=1cm]
       (S) {\textcolor{\colS}
       {$O=\mathbf{0.6}(A+D)$}};

\draw[->,thick,\colC] (C.east)
      -- node[pos=0.5,above]{$A$}
      (S.west);

\node[right of=S,\colS,
      node distance=2.2cm,anchor=west]
      (PV) {$O$};

\draw[->,thick,\colS]
      (S.east) -- (PV.west);

\node[above of=S,anchor=south,\colE,
      node distance=1.2cm]
      (d) {$D=0.2$};

\draw[->,thick,\colE]
      (d.south) -- (S.north);

\node[below of=S,node distance=1cm]
      (fb) {};

\draw[->,thick,\colC]
      ($ (S.east)!0.5!(PV.west) $)
      |- (fb.base) -| (C.south);


\end{tikzpicture}
