\tikzstyle{block} = [draw, rectangle, minimum height=2.5em, minimum width=2.8em]
\tikzstyle{sum} = [draw, circle, node distance=0]
\tikzstyle{input} = [coordinate]
\tikzstyle{output} = [coordinate]
\tikzstyle{junction} =[circle,inner sep=1pt,minimum size=0.1mm,fill=black,draw=black]

\begin{tikzpicture} [auto, node distance=2cm]

\def\colC{green!40!black};
\def\fillC{green!20!white};

\def\colS{magenta!80!black};
\def\fillS{magenta!10!white};

\def\colE{blue!60!black};
\def\fillE{cyan!20!white};

\node[anchor=east]
     (SP) {\textcolor{\colE}{$r(k)$}};

\node[circle,thick,
      right of=SP,node distance=1.2cm,
      draw=\colC,fill=\fillC]
      (fb) {};

\node[block,thick,
      right of=fb,node distance=1.2cm,
      draw=\colC,fill=\fillC]
      (C) {\textcolor{\colC}{$C(z)$}};

\node[circle,thick,
      right of=C,node distance=2.2cm,
      draw=\colS,fill=\fillS]
      (id) {};

\node[above of=id,
      anchor=south,node distance=1cm]
      (d) {\textcolor{\colE}{$d(k)$}};

\node[block,thick,
      right of=id,node distance=1.2cm,
      draw=\colS,fill=\fillS]
      (P) {\textcolor{\colS}{\large{$\frac{1}{z-1}$}}};

\node[right of=P,
      anchor=west,node distance=1.5cm]
      (y) {\textcolor{\colS}{$y(k)$}};

\node[below of=P,node distance=1cm] (fba) {};

\draw[->,\colE,thick]
     (SP.east)
      -- node[pos=0.75,below]{$+$}
     (fb.west);

\draw[->,\colC,thick]
     (fb.east) -- (C.west);

\draw[->,\colC,thick]
     (C.east)
     -- node[pos=0.40,above]{$u(k)$}
        node[pos=0.85,above]{$+$}
     (id.west);

\draw[->,\colE,thick]
     (d.south)
      -- node[pos=0.60,left,xshift=0.5mm]{$+$}
     (id.north);

\draw[->,\colS,thick]
     (id.east) -- (P.west);

\draw[->,\colS,thick]
     (P.east) -- (y.west);

\draw[->,\colC,thick]
     ($(P.east)!0.5!(y.west)$)
     |- (fba.base)
     -| node[pos=0.8,left]{$-$}
     (fb.south);


\end{tikzpicture}
